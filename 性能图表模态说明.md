# 性能与专家权重图表 - 模态说明

## 📊 结论

**`performance_analysis` 文件夹下的所有图表都是针对三模态（RGB/NIR/TIR）训练的模型。**

---

## 🔍 验证依据

### 1. 数据集配置

从日志文件中可以确认，所有 multiscale 实验都使用了 **RGBNT201** 数据集：

```bash
# 日志文件中的配置信息
DATASETS:
  NAMES: RGBNT201
```

### 2. RGBNT201 数据集特性

**RGBNT201** 是一个**三模态跨模态行人重识别数据集**，包含：

- **RGB（可见光）**：提供颜色和纹理信息
- **NIR（近红外）**：提供近红外波段的光谱特征
- **TIR（热红外）**：提供热红外成像的温度分布特征

### 3. 模型架构

从代码和日志可以确认：

1. **数据加载**：每个样本包含三个模态的图像
   ```python
   # data/datasets/bases.py::read_image()
   # 返回: [RGB图像, NI图像, TI图像]
   ```

2. **模型前向传播**：三模态共享同一个 CLIP ViT-B-16 backbone
   ```python
   # modeling/make_model.py::MambaPro.forward()
   # 处理: {'RGB': [B,3,256,128], 'NI': [B,3,256,128], 'TI': [B,3,256,128]}
   ```

3. **特征融合**：多尺度特征从三模态中提取并融合

---

## 📋 图表文件列表

`performance_analysis` 文件夹下的所有图表都来自使用 **RGBNT201 三模态数据集**训练的模型：

| 图表文件 | 对应模型 | 数据集 | 模态数 |
|---------|---------|--------|--------|
| `73.58_8x8+16x16_20251217_175523_performance_expert_weights.png` | 8×8+16×16 多尺度 | RGBNT201 | 3（RGB/NIR/TIR） |
| `75.28_8x8_20251217_131802_performance_expert_weights.png` | 8×8 多尺度 | RGBNT201 | 3（RGB/NIR/TIR） |
| `76.13_4x4_20251217_121738_performance_expert_weights.png` | 4×4 多尺度 | RGBNT201 | 3（RGB/NIR/TIR） |
| `76.79_4x4+8x8_20251217_151621_performance_expert_weights.png` | 4×4+8×8 多尺度 | RGBNT201 | 3（RGB/NIR/TIR） |
| `77.76_4x4+16x16_20251217_160700_performance_expert_weights.png` | 4×4+16×16 多尺度 | RGBNT201 | 3（RGB/NIR/TIR） |
| `78.3mAP_1212_1331_run_20251212_130937_performance_expert_weights.png` | 4×4+8×8+16×16 多尺度 | RGBNT201 | 3（RGB/NIR/TIR） |
| `79.4mAP_performance_expert_weights.png` | 完整多尺度 | RGBNT201 | 3（RGB/NIR/TIR） |

---

## 🔍 验证方法

### 方法 1: 检查日志文件

```bash
# 查看数据集配置
grep -E "DATASETS|NAMES|RGBNT" /path/to/train_*.log | head -5

# 输出示例：
# DATASETS:
#   NAMES: RGBNT201
```

### 方法 2: 检查脚本源码

查看 `visualize_performance_expert_weights.py` 和 `batch_visualize_multiscale_performance.py`：

- **脚本功能**：从训练日志中提取性能指标（mAP、Rank-1）和专家权重
- **数据来源**：`outputs/multiscale/*/logs/train_*.log`
- **日志内容**：包含 `DATASETS.NAMES: RGBNT201` 配置

### 方法 3: 检查模型配置

所有 multiscale 实验都使用相同的配置文件，其中指定了：

```yaml
DATASETS:
  NAMES: ('RGBNT201',)
  ROOT_DIR: ('/path/to/datasets',)
```

---

## 📊 性能指标含义

### mAP 和 Rank-1

这些图表中的性能指标（mAP、Rank-1）是**三模态跨模态检索**的结果：

- **查询集**：可能包含 RGB、NIR 或 TIR 模态的图像
- **图库集**：包含 RGB、NIR 和 TIR 三种模态的图像
- **检索任务**：跨模态匹配（例如：RGB 查询 → NIR/TIR 图库）

### 专家权重

专家权重表示模型在融合**三模态多尺度特征**时，对不同尺度的依赖程度：

- **Scale 4×4**：细粒度特征（适合细节匹配）
- **Scale 8×8**：中等粒度特征（平衡细节和全局）
- **Scale 16×16**：粗粒度特征（适合全局匹配）

---

## 🎯 关键结论

1. **所有图表都是三模态的**：使用 RGBNT201 数据集（RGB/NIR/TIR）
2. **性能指标是跨模态的**：mAP 和 Rank-1 反映跨模态检索性能
3. **专家权重反映多尺度融合**：展示模型如何融合三模态的多尺度特征

---

## 📝 注意事项

如果将来需要生成**单模态**的性能图表，需要：

1. **使用单模态数据集**（如仅 RGB 的数据集）
2. **修改数据加载器**：只加载单一模态
3. **重新训练模型**：使用单模态配置
4. **生成新的日志文件**：包含单模态验证结果
5. **使用相同的可视化脚本**：脚本本身不区分模态，只从日志中提取数据

---

## 🔗 相关文档

- **数据集说明**: `Readme合集/9-RGBNT201配置解读.md`
- **数据流解析**: `整体项目结构图/2-整体流程及代码分析.md`
- **可视化脚本**: `visualize_performance_expert_weights.py`
- **批量处理脚本**: `batch_visualize_multiscale_performance.py`

---

**最后更新**: 2025-12-21

