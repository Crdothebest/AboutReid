# 热力图可视化关键代码

## 1. EigenCAM 核心生成逻辑 (`grad_cam.py`)

### 位置：`EigenCAM.generate_cam()` 方法（约第608-665行）

```python
# EigenCAM: 使用 PCA 找到主成分
# 将特征图 reshape 为 [C, H*W]
features = activations.view(C, H * W)  # [C, H*W]

# 计算协方差矩阵
features_mean = features.mean(dim=1, keepdim=True)  # [C, 1]
features_centered = features - features_mean  # [C, H*W]

# 协方差矩阵: [C, C] = [C, H*W] @ [H*W, C]
covariance = torch.matmul(features_centered, features_centered.t()) / (H * W - 1)

# 计算主特征向量（使用 SVD 或 eigendecomposition）
try:
    # 使用 SVD 更稳定
    U, S, V = torch.svd(covariance)
    # 第一个主成分（最大特征值对应的特征向量）
    principal_component = U[:, 0]  # [C]
except:
    # 如果 SVD 失败，使用简单的平均
    principal_component = features_mean.squeeze(1)

# 投影到主成分方向
# cam: [H*W] = [C] @ [C, H*W]
cam = torch.matmul(principal_component.unsqueeze(0), features)  # [1, H*W]
cam = cam.squeeze(0)  # [H*W]

# Reshape 回 2D
cam = cam.view(H, W)  # [H, W]

# 🔥 关键修正1：取绝对值
# 主成分方向具有任意性，特征值可能是负数，需要取绝对值
# Heatmap = |Principal_Component|
cam = torch.abs(cam)

# 🔥 关键修正2：ReLU激活（只保留正向贡献的特征）
cam = F.relu(cam)

# 🔥 关键修正3：归一化到 [0, 1]
# 确保值域在 [0, 1] 之间，用于颜色映射
# 公式：Heatmap = (Heatmap - min) / (max - min)
cam_min = cam.min()
cam_max = cam.max()
if cam_max > cam_min:
    cam = (cam - cam_min) / (cam_max - cam_min)
else:
    cam = torch.zeros_like(cam)

# 转换为 numpy
cam = cam.cpu().numpy()

# 🔥 关键修正4：颜色映射反转
# 标准热力图：背景冷色（蓝），特征强处暖色（红）
# cv2.COLORMAP_JET: 蓝色(低值) -> 青色 -> 绿色 -> 黄色 -> 红色(高值)
# 如果发现结果是反的（背景是红色，目标是蓝色），需要反转
# 根据用户反馈，当前RGB/NIR的背景是大片红色，目标反而是蓝色，说明需要反转
cam = 1.0 - cam  # 反转：确保背景是蓝色（冷色），特征强处是红色（暖色）

return cam
```

---

## 2. 多模态热力图可视化处理流程 (`generate_heatmap_visualization.py`)

### 位置：`generate_multimodal_heatmap()` 函数（约第173-322行）

### 第一步：收集所有模态的原始热力图（用于统一归一化）

```python
# 🔥 第一步：收集所有模态的原始热力图，用于统一归一化
raw_heatmaps = {}  # 存储原始热力图（归一化前）
original_images_dict = {}  # 存储原始图像

for mod, mod_name in zip(modalities, modality_names):
    if mod not in images:
        continue
    
    original_image, pil_image = images[mod]
    original_images_dict[mod] = original_image
    
    # 预处理
    img_tensor = transform(pil_image).unsqueeze(0).to(device)
    input_dict = {
        'RGB': torch.zeros_like(img_tensor),
        'NI': torch.zeros_like(img_tensor),
        'TI': torch.zeros_like(img_tensor)
    }
    input_dict[mod] = img_tensor
    
    # 准备标签
    cam_label = torch.tensor([0]).to(device)
    view_label = torch.tensor([0]).to(device)
    
    # 生成原始热力图（未归一化）
    try:
        if method.lower() == 'eigencam':
            raw_heatmap = cam.generate_cam(
                input_dict,
                cam_label=cam_label,
                view_label=view_label
            )
            raw_heatmaps[mod] = raw_heatmap
        else:
            # GradCAM 也收集原始热力图
            heatmap, _ = cam.generate_gradcam(
                input_dict, original_image, target_class=None, alpha=0.0,
                cam_label=cam_label, view_label=view_label
            )
            raw_heatmaps[mod] = heatmap
    except Exception as e:
        print(f"  ⚠️  生成 {mod_name} 模态热力图失败: {e}")
        continue
```

### 第二步：统一归一化（使用全模态的最大激活值）

```python
# 🔥 第二步：统一归一化（使用全模态的最大激活值）
if raw_heatmaps:
    global_max = max(h.max() for h in raw_heatmaps.values())
    global_min = min(h.min() for h in raw_heatmaps.values())
    print(f"  📊 全局归一化范围: [{global_min:.4f}, {global_max:.4f}]")
else:
    global_max = 1.0
    global_min = 0.0
```

### 第三步：为每种模态生成可视化（EigenCAM 处理）

```python
# 🔥 第三步：为每种模态生成可视化
for row, (mod, mod_name) in enumerate(zip(modalities, modality_names)):
    if mod not in images or mod not in raw_heatmaps:
        # 如果缺少某个模态，显示空白
        axes[row, 0].axis('off')
        axes[row, 1].axis('off')
        continue
    
    original_image = original_images_dict[mod]
    heatmap = raw_heatmaps[mod].copy()
    
    # 🔥 EigenCAM 美化处理
    if method.lower() == 'eigencam':
        # 1. 🔥 硬阈值过滤：去除边缘伪影（Edge Artifacts）
        # 将激活值低于最大值30%的像素点强制设为0
        threshold_ratio = 0.3
        threshold_value = heatmap.max() * threshold_ratio
        heatmap[heatmap < threshold_value] = 0
        
        # 2. 重新归一化（使用全局最大值）
        if global_max > global_min:
            heatmap = (heatmap - global_min) / (global_max - global_min + 1e-8)
        else:
            heatmap = np.zeros_like(heatmap)
        heatmap = np.clip(heatmap, 0, 1)
        
        # 3. 放大：使用双三次插值（Bicubic）提升平滑度
        heatmap_resized = cv2.resize(
            heatmap,
            (original_image.shape[1], original_image.shape[0]),
            interpolation=cv2.INTER_CUBIC
        )
        
        # 4. 高斯模糊：消除生硬边缘，实现"云雾感"效果
        heatmap_blurred = cv2.GaussianBlur(heatmap_resized, (21, 21), 0)
        
        # 5. 重新归一化（确保值域在 [0, 1]）
        heatmap_max = heatmap_blurred.max()
        if heatmap_max > 0:
            heatmap_blurred = heatmap_blurred / heatmap_max
        else:
            heatmap_blurred = np.zeros_like(heatmap_blurred)
        
        # 6. 🔥 颜色映射：确保背景是蓝色（冷色），特征强处是红色（暖色）
        # cv2.COLORMAP_JET: 蓝色(低值) -> 青色 -> 绿色 -> 黄色 -> 红色(高值)
        heatmap_normalized = np.clip(heatmap_blurred, 0, 1)
        heatmap_uint8 = np.uint8(255 * heatmap_normalized)
        
        # 应用颜色映射
        heatmap_colored = cv2.applyColorMap(heatmap_uint8, cv2.COLORMAP_JET)
        heatmap_colored = cv2.cvtColor(heatmap_colored, cv2.COLOR_BGR2RGB)
        
        # 7. 叠加：使用alpha透明度叠加在原始图像上
        overlay = (
            heatmap_colored * alpha + original_image.astype(np.float32) * (1 - alpha)
        ).astype(np.uint8)
```

---

## 关键问题检查点

### 1. 颜色映射问题
- **当前实现**：在 `grad_cam.py` 第663行执行 `cam = 1.0 - cam` 反转
- **检查**：如果背景是红色、目标是蓝色，说明反转方向错误，应该移除这行或再次反转

### 2. 归一化问题
- **当前实现**：先单独归一化（在 `generate_cam` 中），再全局归一化（在 `generate_multimodal_heatmap` 中）
- **潜在问题**：`generate_cam` 已经归一化到 [0,1]，所以 `global_max` 和 `global_min` 可能都是 1.0 和 0.0
- **建议**：应该在 `generate_cam` 中返回未归一化的原始值，或者修改归一化逻辑

### 3. 阈值过滤问题
- **当前实现**：在全局归一化之前应用阈值（第242行）
- **潜在问题**：如果 `generate_cam` 已经归一化，阈值应该基于归一化后的值

### 4. 重复归一化问题
- **当前实现**：多次归一化（`generate_cam` 中一次，全局归一化一次，模糊后再归一化一次）
- **潜在问题**：可能导致信息丢失或数值不稳定

---

## 建议的修正方向

1. **修改 `generate_cam` 返回未归一化的原始值**，或者添加参数控制是否归一化
2. **调整归一化顺序**：先收集所有模态的原始值，再统一归一化
3. **检查颜色映射**：确认 `cam = 1.0 - cam` 的方向是否正确
4. **优化阈值过滤**：在正确的归一化阶段应用阈值
