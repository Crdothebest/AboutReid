# 📊 Top-k 路由输出示例

## 📋 输出说明

启用 Top-k 路由后，Linux 输出会显示详细的路由信息，包括：
- ✅ Top-k 路由状态（启用/禁用）
- ✅ Top-k 参数（k 值、模式）
- ✅ 路由前后的权重分布对比
- ✅ 激活的专家索引和对应尺度

---

## 🎯 示例 1：启用 Top-2 软路由

### 配置
```bash
MODEL.MOE_USE_TOP_K_ROUTING True
MODEL.MOE_TOP_K 2
MODEL.MOE_TOP_K_MODE "soft"
```

### 输出示例

```
🎯 CLIP多尺度MoE模块启动！
   - 输入patch tokens形状: torch.Size([32, 128, 512])
   - 滑动窗口尺度: [4, 8, 16]
   - 特征维度: 512

🚀 多尺度MoE模块启动！
   - 输入特征数量: 3
   - 每个特征形状: torch.Size([32, 512])
   - 滑动窗口尺度: [4, 8, 16]
   - 专家数量: 3
   - 门控加权-预处理机制: 已禁用
   - 注意力-预处理机制: 已禁用
   - Top-k 路由机制: ✅ 已启用 (k=2, mode=soft)

🎯 拼接融合：使用无预处理（无预处理）
   - 拼接方式: torch.cat() 直接拼接
   - 拼接维度: feat_dim * num_scales = 512 * 3 = 1536
   - 步骤：直接拼接 → 门控网络计算专家权重 → 专家网络处理 → 加权融合

🎯 专家网络处理：使用门控网络和专家网络
   - 专家数量: 3
   - 专家隐藏层维度: 1280
   - 专家层数: 2
   - 门控网络计算专家权重
   - Top-k 路由: 将强制激活 Top-2 专家 (模式: soft)
   - 专家网络处理多尺度特征
   - 加权融合得到最终特征

🎯 门控网络处理：计算专家权重
   - 输入特征形状: torch.Size([32, 1536])
   - 输出权重形状: [32, 3]
   - 说明：根据输入特征动态计算专家权重，权重会随训练优化
   - ⚠️  注意：Top-k 路由将在此后处理，只保留 Top-2 专家的权重

🎯 Top-k 路由处理：强制激活 Top-2 专家
   - 输入权重形状: torch.Size([32, 3])
   - Top-k 值: 2 (将激活 2/3 个专家)
   - 路由模式: soft
   - 专家总数: 3
   - 原始权重分布（第一个样本）: [0.6234, 0.2456, 0.1310]

✅ 软 Top-k 路由完成
   - 输出权重形状: torch.Size([32, 3])
   - Top-2 专家权重已重新归一化
   - 非 Top-2 专家权重已设为 0（但保留梯度）
   - 路由后权重分布（第一个样本）: [0.7174, 0.2826, 0.0000]
   - 激活的专家索引: [0, 1] (对应尺度: [4, 8])
   - 说明：保留被屏蔽专家的梯度，减少与损失函数的冲突

🧠 专家网络开始处理特征: torch.Size([32, 512])
🧠 专家网络开始处理特征: torch.Size([32, 512])
🧠 专家网络开始处理特征: torch.Size([32, 512])

🎯 最终融合：专家输出融合
   - 融合特征形状: torch.Size([32, 512])
   - 最终特征形状: [B, 512]
```

---

## 🎯 示例 2：禁用 Top-k 路由（传统软路由）

### 配置
```bash
MODEL.MOE_USE_TOP_K_ROUTING False
```

### 输出示例

```
🎯 CLIP多尺度MoE模块启动！
   - 输入patch tokens形状: torch.Size([32, 128, 512])
   - 滑动窗口尺度: [4, 8, 16]
   - 特征维度: 512

🚀 多尺度MoE模块启动！
   - 输入特征数量: 3
   - 每个特征形状: torch.Size([32, 512])
   - 滑动窗口尺度: [4, 8, 16]
   - 专家数量: 3
   - 门控加权-预处理机制: 已禁用
   - 注意力-预处理机制: 已禁用
   - Top-k 路由机制: ❌ 已禁用 (使用传统软路由)

🎯 拼接融合：使用无预处理（无预处理）
   - 拼接方式: torch.cat() 直接拼接
   - 拼接维度: feat_dim * num_scales = 512 * 3 = 1536
   - 步骤：直接拼接 → 门控网络计算专家权重 → 专家网络处理 → 加权融合

🎯 专家网络处理：使用门控网络和专家网络
   - 专家数量: 3
   - 专家隐藏层维度: 1280
   - 专家层数: 2
   - 门控网络计算专家权重
   - Top-k 路由: 未启用 (所有专家都有非零权重)
   - 专家网络处理多尺度特征
   - 加权融合得到最终特征

🎯 门控网络处理：计算专家权重
   - 输入特征形状: torch.Size([32, 1536])
   - 输出权重形状: [32, 3]
   - 说明：根据输入特征动态计算专家权重，权重会随训练优化

📊 传统软路由权重分布（第一个样本）: [0.6234, 0.2456, 0.1310]
   - 说明：所有专家都有非零权重，权重通过 softmax 归一化

🧠 专家网络开始处理特征: torch.Size([32, 512])
🧠 专家网络开始处理特征: torch.Size([32, 512])
🧠 专家网络开始处理特征: torch.Size([32, 512])

🎯 最终融合：专家输出融合
   - 融合特征形状: torch.Size([32, 512])
   - 最终特征形状: [B, 512]
```

---

## 🎯 示例 3：启用 Top-2 硬路由

### 配置
```bash
MODEL.MOE_USE_TOP_K_ROUTING True
MODEL.MOE_TOP_K 2
MODEL.MOE_TOP_K_MODE "hard"
```

### 输出示例（关键部分）

```
🎯 Top-k 路由处理：强制激活 Top-2 专家
   - 输入权重形状: torch.Size([32, 3])
   - Top-k 值: 2 (将激活 2/3 个专家)
   - 路由模式: hard
   - 专家总数: 3
   - 原始权重分布（第一个样本）: [0.6234, 0.2456, 0.1310]

✅ 硬 Top-k 路由完成
   - 输出权重形状: torch.Size([32, 3])
   - Top-2 专家权重已重新归一化
   - 非 Top-2 专家权重已设为 0（完全屏蔽梯度）
   - 路由后权重分布（第一个样本）: [0.7174, 0.2826, 0.0000]
   - 激活的专家索引: [0, 1] (对应尺度: [4, 8])
   - ⚠️  警告：可能丢失关键信息，与损失函数冲突更严重
```

---

## 📊 输出信息解读

### 1. 路由状态显示

**位置**：`🚀 多尺度MoE模块启动！` 部分
- ✅ **已启用**：`Top-k 路由机制: ✅ 已启用 (k=2, mode=soft)`
- ❌ **已禁用**：`Top-k 路由机制: ❌ 已禁用 (使用传统软路由)`

### 2. 路由处理信息

**位置**：`🎯 专家网络处理` 部分
- **启用时**：`Top-k 路由: 将强制激活 Top-2 专家 (模式: soft)`
- **禁用时**：`Top-k 路由: 未启用 (所有专家都有非零权重)`

### 3. 门控网络提示

**位置**：`🎯 门控网络处理` 部分
- **启用时**：显示 `⚠️  注意：Top-k 路由将在此后处理，只保留 Top-2 专家的权重`

### 4. Top-k 路由处理详情

**位置**：`🎯 Top-k 路由处理` 部分
- 显示 Top-k 参数（k 值、模式、专家总数）
- 显示原始权重分布（第一个样本）
- 显示路由后权重分布（第一个样本）
- 显示激活的专家索引和对应尺度

### 5. 传统软路由信息

**位置**：`📊 传统软路由权重分布` 部分（仅禁用 Top-k 时显示）
- 显示所有专家的权重分布
- 说明所有专家都有非零权重

---

## 🔍 关键信息对比

### 启用 Top-2 软路由 vs 禁用 Top-k 路由

| 信息项 | Top-2 软路由 | 传统软路由 |
|--------|-------------|-----------|
| 路由状态 | ✅ 已启用 (k=2, mode=soft) | ❌ 已禁用 |
| 权重分布 | [0.7174, 0.2826, 0.0000] | [0.6234, 0.2456, 0.1310] |
| 激活专家 | 2 个（索引 0, 1） | 3 个（全部） |
| 梯度保留 | ✅ 保留（软模式） | ✅ 全部保留 |

---

## 💡 使用建议

1. **查看路由状态**：在 `🚀 多尺度MoE模块启动！` 部分确认 Top-k 路由是否启用
2. **检查权重分布**：在 `🎯 Top-k 路由处理` 部分查看路由前后的权重对比
3. **验证激活专家**：确认激活的专家索引和对应尺度是否符合预期
4. **监控训练过程**：观察权重分布是否合理，避免某个专家被完全屏蔽

---

**最后更新**：2025-01-XX  
**版本**：V24.1

