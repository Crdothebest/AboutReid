# 基于现有.pth文件重新运行实验 - 具体操作命令

## 🚀 快速开始命令

### 1. 检查现有模型文件
```bash
# 查看现有.pth文件
ls -la /Users/a11/Desktop/AboutReid/论文最好模型/

# 检查文件大小和修改时间
stat /Users/a11/Desktop/AboutReid/论文最好模型/MambaProbest.pth
stat /Users/a11/Desktop/AboutReid/论文最好模型/MambaPro_60.pth
```

### 2. 备份原始配置
```bash
# 备份原始配置文件
cp configs/RGBNT201/MambaPro_moe.yml configs/RGBNT201/MambaPro_moe.yml.backup
cp configs/RGBNT201/baseline.yml configs/RGBNT201/baseline.yml.backup
```

## 📋 场景一：从断点继续训练

### 步骤1：创建继续训练的配置文件
```bash
# 复制配置文件
cp configs/RGBNT201/MambaPro_moe.yml configs/RGBNT201/MambaPro_resume.yml

# 修改预训练路径（使用第60轮权重继续训练）
sed -i 's|PRETRAIN_PATH_T:.*|PRETRAIN_PATH_T: "/Users/a11/Desktop/AboutReid/论文最好模型/MambaPro_60.pth"|' configs/RGBNT201/MambaPro_resume.yml

# 修改输出目录
sed -i 's|OUTPUT_DIR:.*|OUTPUT_DIR: "/Users/a11/Desktop/AboutReid/outputs/resume_experiment"|' configs/RGBNT201/MambaPro_resume.yml

# 增加训练轮数（从60轮继续到80轮）
sed -i 's|MAX_EPOCHS: 60|MAX_EPOCHS: 80|' configs/RGBNT201/MambaPro_resume.yml
```

### 步骤2：执行继续训练
```bash
# 使用智能实验记录脚本
./run_experiment.sh --config_file configs/RGBNT201/MambaPro_resume.yml

# 或者直接使用train_net.py
python train_net.py --config_file configs/RGBNT201/MambaPro_resume.yml
```

## 📋 场景二：基于最佳权重微调

### 步骤1：创建微调配置文件
```bash
# 复制配置文件
cp configs/RGBNT201/MambaPro_moe.yml configs/RGBNT201/MambaPro_finetune.yml

# 修改预训练路径（使用最佳权重）
sed -i 's|PRETRAIN_PATH_T:.*|PRETRAIN_PATH_T: "/Users/a11/Desktop/AboutReid/论文最好模型/MambaProbest.pth"|' configs/RGBNT201/MambaPro_finetune.yml

# 修改输出目录
sed -i 's|OUTPUT_DIR:.*|OUTPUT_DIR: "/Users/a11/Desktop/AboutReid/outputs/finetune_experiment"|' configs/RGBNT201/MambaPro_finetune.yml

# 降低学习率（微调）
sed -i 's|BASE_LR: 0.0005|BASE_LR: 0.0001|' configs/RGBNT201/MambaPro_finetune.yml

# 减少训练轮数
sed -i 's|MAX_EPOCHS: 60|MAX_EPOCHS: 20|' configs/RGBNT201/MambaPro_finetune.yml

# 减少预热轮数
sed -i 's|WARMUP_ITERS: 20|WARMUP_ITERS: 5|' configs/RGBNT201/MambaPro_finetune.yml
```

### 步骤2：执行微调训练
```bash
# 使用智能实验记录脚本
./run_experiment.sh --config_file configs/RGBNT201/MambaPro_finetune.yml

# 或者带参数覆盖
./run_experiment.sh --config_file configs/RGBNT201/MambaPro_finetune.yml \
  SOLVER.BASE_LR 0.00005 \
  SOLVER.MAX_EPOCHS 15
```

## 📋 场景三：仅测试评估

### 步骤1：创建测试配置文件
```bash
# 复制配置文件
cp configs/RGBNT201/MambaPro_moe.yml configs/RGBNT201/MambaPro_test.yml

# 修改测试权重路径
sed -i 's|WEIGHT:.*|WEIGHT: "/Users/a11/Desktop/AboutReid/论文最好模型/MambaProbest.pth"|' configs/RGBNT201/MambaPro_test.yml

# 修改输出目录
sed -i 's|OUTPUT_DIR:.*|OUTPUT_DIR: "/Users/a11/Desktop/AboutReid/outputs/test_experiment"|' configs/RGBNT201/MambaPro_test.yml
```

### 步骤2：执行测试
```bash
# 使用test_net.py进行测试
python test_net.py --config_file configs/RGBNT201/MambaPro_test.yml

# 或者使用train_net.py进行测试（会跳过训练，直接测试）
python train_net.py --config_file configs/RGBNT201/MambaPro_test.yml
```

## 🔧 高级操作命令

### 1. 批量创建不同配置的实验
```bash
# 创建多个实验配置
for epoch in 30 40 50; do
  cp configs/RGBNT201/MambaPro_moe.yml configs/RGBNT201/MambaPro_${epoch}epoch.yml
  sed -i "s|MAX_EPOCHS: 60|MAX_EPOCHS: $epoch|" configs/RGBNT201/MambaPro_${epoch}epoch.yml
  sed -i "s|OUTPUT_DIR:.*|OUTPUT_DIR: \"/Users/a11/Desktop/AboutReid/outputs/${epoch}epoch_experiment\"|" configs/RGBNT201/MambaPro_${epoch}epoch.yml
done
```

### 2. 使用不同学习率进行实验
```bash
# 创建不同学习率的实验
for lr in 0.0001 0.0005 0.001; do
  cp configs/RGBNT201/MambaPro_moe.yml configs/RGBNT201/MambaPro_lr${lr}.yml
  sed -i "s|BASE_LR: 0.0005|BASE_LR: $lr|" configs/RGBNT201/MambaPro_lr${lr}.yml
  sed -i "s|OUTPUT_DIR:.*|OUTPUT_DIR: \"/Users/a11/Desktop/AboutReid/outputs/lr${lr}_experiment\"|" configs/RGBNT201/MambaPro_lr${lr}.yml
done
```

### 3. 使用智能实验记录脚本的高级功能
```bash
# 带参数覆盖的实验
./run_experiment.sh --config_file configs/RGBNT201/MambaPro_moe.yml \
  MODEL.MOE_EXPERT_HIDDEN_DIM 1024 \
  MODEL.MOE_TEMPERATURE 0.7 \
  SOLVER.MAX_EPOCHS 30 \
  SOLVER.BASE_LR 0.0001

# 启用门控融合的实验
./run_experiment.sh --config_file configs/RGBNT201/MambaPro_moe.yml \
  MODEL.USE_GATE_FUSION True \
  MODEL.GATE_NUM_HEADS 8 \
  MODEL.GATE_DROPOUT 0.1

# 禁用MoE的实验
./run_experiment.sh --config_file configs/RGBNT201/MambaPro_moe.yml \
  MODEL.USE_MULTI_SCALE_MOE False
```

## 📊 监控和结果分析命令

### 1. 实时监控训练过程
```bash
# 监控训练日志
tail -f outputs/resume_experiment/train_log.txt

# 监控GPU使用情况
watch -n 1 nvidia-smi

# 监控系统资源
htop
```

### 2. 分析训练结果
```bash
# 查看最佳结果
grep "Best mAP\|Best Rank-1" outputs/resume_experiment/train_log.txt

# 查看专家权重分布
grep "专家权重" outputs/resume_experiment/train_log.txt

# 查看损失曲线
grep "Loss" outputs/resume_experiment/train_log.txt | tail -20
```

### 3. 对比不同实验结果
```bash
# 使用可视化脚本
python visualize_reid_retrieval.py --config_file configs/RGBNT201/MambaPro_resume.yml

# 使用结果分析脚本
python analyze_results.py --experiment_dir outputs/resume_experiment
```

## 🛠️ 故障排除命令

### 1. 检查模型文件完整性
```bash
# 检查.pth文件是否可以正常加载
python -c "
import torch
try:
    model = torch.load('/Users/a11/Desktop/AboutReid/论文最好模型/MambaProbest.pth', map_location='cpu')
    print('✅ 模型文件加载成功')
    print('模型键数量:', len(model.keys()))
    print('前5个键:', list(model.keys())[:5])
except Exception as e:
    print('❌ 模型文件加载失败:', e)
"
```

### 2. 检查配置文件格式
```bash
# 检查YAML格式是否正确
python -c "
import yaml
try:
    with open('configs/RGBNT201/MambaPro_resume.yml', 'r') as f:
        config = yaml.safe_load(f)
    print('✅ 配置文件格式正确')
except Exception as e:
    print('❌ 配置文件格式错误:', e)
"
```

### 3. 检查CUDA环境
```bash
# 检查CUDA是否可用
python -c "
import torch
print('CUDA可用:', torch.cuda.is_available())
print('CUDA设备数量:', torch.cuda.device_count())
if torch.cuda.is_available():
    print('当前设备:', torch.cuda.current_device())
    print('设备名称:', torch.cuda.get_device_name(0))
"
```

## 📝 一键执行脚本

### 创建一键执行脚本
```bash
# 创建一键执行脚本
cat > resume_experiment.sh << 'EOF'
#!/bin/bash

echo "🚀 开始基于现有.pth文件重新运行实验"

# 检查模型文件
if [ ! -f "/Users/a11/Desktop/AboutReid/论文最好模型/MambaProbest.pth" ]; then
    echo "❌ 模型文件不存在"
    exit 1
fi

# 创建配置文件
cp configs/RGBNT201/MambaPro_moe.yml configs/RGBNT201/MambaPro_resume.yml

# 修改配置
sed -i 's|PRETRAIN_PATH_T:.*|PRETRAIN_PATH_T: "/Users/a11/Desktop/AboutReid/论文最好模型/MambaProbest.pth"|' configs/RGBNT201/MambaPro_resume.yml
sed -i 's|OUTPUT_DIR:.*|OUTPUT_DIR: "/Users/a11/Desktop/AboutReid/outputs/resume_experiment"|' configs/RGBNT201/MambaPro_resume.yml

# 执行实验
echo "🏃 开始执行实验..."
./run_experiment.sh --config_file configs/RGBNT201/MambaPro_resume.yml

echo "✅ 实验完成"
EOF

# 给脚本执行权限
chmod +x resume_experiment.sh

# 执行脚本
./resume_experiment.sh
```

## 🎯 最佳实践建议

1. **总是先备份**：在修改配置前先备份原始文件
2. **逐步验证**：先进行小规模测试，确认配置正确
3. **监控资源**：注意GPU内存和训练时间
4. **记录实验**：使用智能实验记录脚本自动记录结果
5. **对比分析**：与原始结果进行对比，验证改进效果

这些命令可以帮助您基于现有的.pth文件重新运行实验。根据您的具体需求选择合适的场景和命令。
