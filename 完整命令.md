# 生成热力图可视化的完整 Linux 命令

## 🚀 方法1：使用 Shell 脚本（推荐）

```bash
cd /home/zhanghaoyang/Desktop/yzy/AboutReid
bash run_heatmap_command.sh
```

## 🚀 方法2：单行命令（完整版）

```bash
eval "$(conda shell.bash hook)" && conda activate MambaPro && cd /home/zhanghaoyang/Desktop/yzy/AboutReid && python generate_heatmap_visualization.py --weight_path /home/zhanghaoyang/Desktop/yzy/AboutReid/pths/ViT-B-16.pt --config_file /home/zhanghaoyang/Desktop/yzy/MambaPro/configs/RGBNT201/MambaPro.yml --query_id 000274 --dataset_root /home/zhanghaoyang/Desktop/yzy/MambaPro/data/datasets/RGBNT201 --output_path heatmap_000274.png --alpha 0.4
```

## 🚀 方法3：分步执行（便于调试）

```bash
# 步骤1：激活 conda 环境
eval "$(conda shell.bash hook)"
conda activate MambaPro

# 步骤2：切换到脚本目录
cd /home/zhanghaoyang/Desktop/yzy/AboutReid

# 步骤3：运行脚本
python generate_heatmap_visualization.py \
    --weight_path /home/zhanghaoyang/Desktop/yzy/AboutReid/pths/ViT-B-16.pt \
    --config_file /home/zhanghaoyang/Desktop/yzy/MambaPro/configs/RGBNT201/MambaPro.yml \
    --query_id 000274 \
    --dataset_root /home/zhanghaoyang/Desktop/yzy/MambaPro/data/datasets/RGBNT201 \
    --output_path heatmap_000274.png \
    --alpha 0.4
```

## 📝 参数说明

| 参数 | 值 | 说明 |
|------|-----|------|
| `--weight_path` | `/home/zhanghaoyang/Desktop/yzy/AboutReid/pths/ViT-B-16.pt` | 模型权重文件路径 |
| `--config_file` | `/home/zhanghaoyang/Desktop/yzy/MambaPro/configs/RGBNT201/MambaPro.yml` | 配置文件路径 |
| `--query_id` | `000274` | 查询人员ID |
| `--dataset_root` | `/home/zhanghaoyang/Desktop/yzy/MambaPro/data/datasets/RGBNT201` | 数据集根目录 |
| `--output_path` | `heatmap_000274.png` | 输出图像路径 |
| `--alpha` | `0.4` | 热力图透明度（可选，默认0.4） |

## ⚠️ 重要提示

### 如果权重文件是 TorchScript 模型（.pt 文件）

如果看到以下错误提示：
```
❌ 错误：检测到 TorchScript 模型（.pt 文件）
   TorchScript 模型不支持 Grad-CAM 热力图生成
```

**解决方案**：需要使用 PyTorch 权重文件（.pth 文件）

### 使用 .pth 权重文件的命令

```bash
eval "$(conda shell.bash hook)" && conda activate MambaPro && cd /home/zhanghaoyang/Desktop/yzy/AboutReid && python generate_heatmap_visualization.py --weight_path /path/to/model.pth --config_file /home/zhanghaoyang/Desktop/yzy/MambaPro/configs/RGBNT201/MambaPro.yml --query_id 000274 --dataset_root /home/zhanghaoyang/Desktop/yzy/MambaPro/data/datasets/RGBNT201 --output_path heatmap_000274.png --alpha 0.4
```

## 🔍 自定义参数

### 修改查询人员ID

```bash
# 将 --query_id 000274 改为其他ID，例如 000123
python generate_heatmap_visualization.py \
    --weight_path /path/to/model.pth \
    --config_file /home/zhanghaoyang/Desktop/yzy/MambaPro/configs/RGBNT201/MambaPro.yml \
    --query_id 000123 \
    --dataset_root /home/zhanghaoyang/Desktop/yzy/MambaPro/data/datasets/RGBNT201 \
    --output_path heatmap_000123.png
```

### 修改输出路径

```bash
# 将输出保存到指定目录
python generate_heatmap_visualization.py \
    --weight_path /path/to/model.pth \
    --config_file /home/zhanghaoyang/Desktop/yzy/MambaPro/configs/RGBNT201/MambaPro.yml \
    --query_id 000274 \
    --dataset_root /home/zhanghaoyang/Desktop/yzy/MambaPro/data/datasets/RGBNT201 \
    --output_path outputs/heatmaps/heatmap_000274.png
```

### 调整热力图透明度

```bash
# alpha 值范围：0.0（完全透明）到 1.0（完全不透明）
# 默认值：0.4
python generate_heatmap_visualization.py \
    --weight_path /path/to/model.pth \
    --config_file /home/zhanghaoyang/Desktop/yzy/MambaPro/configs/RGBNT201/MambaPro.yml \
    --query_id 000274 \
    --dataset_root /home/zhanghaoyang/Desktop/yzy/MambaPro/data/datasets/RGBNT201 \
    --output_path heatmap_000274.png \
    --alpha 0.6  # 更不透明，热力图更明显
```

## 📊 输出结果

成功运行后，会在当前目录生成 `heatmap_000274.png` 文件，包含：
- 3行×2列布局
- 左列：RGB、NIR、TIR 原始图像
- 右列：对应模态叠加了热力图的图像

## 🐛 常见问题

### 1. conda 环境未激活

**错误**：`ModuleNotFoundError: No module named 'torch'`

**解决**：确保激活了 MambaPro 环境
```bash
eval "$(conda shell.bash hook)"
conda activate MambaPro
```

### 2. 文件路径不存在

**错误**：`❌ 权重文件不存在` 或 `❌ 配置文件不存在`

**解决**：检查文件路径是否正确，使用绝对路径

### 3. 数据集路径错误

**错误**：`❌ 数据集路径不存在`

**解决**：确认数据集目录结构为：
```
{dataset_root}/
├── test/
│   ├── RGB/
│   ├── NI/
│   └── TI/
```
