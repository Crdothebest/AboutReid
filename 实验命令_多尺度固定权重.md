# 多尺度（4、8、16）+ 固定权重实验命令

## 🎯 实验目标

使用多尺度滑动窗口（4×4、8×8、16×16）提取特征，使用 MOE 专家网络进行专业化处理，但**使用固定权重**而非动态门控网络进行融合。

---

## 📋 方案说明

### 核心特点

1. **多尺度特征提取**：✅ 启用（4×4、8×8、16×16）
2. **MOE 专家网络**：✅ 启用（三个专家网络分别处理三个尺度）
3. **门控网络**：❌ 禁用（使用固定权重替代）
4. **权重分配**：固定不变（不随输入变化）

### 实现原理

```
多尺度特征提取 (4×4, 8×8, 16×16)
    ↓
专家网络处理
    ├─ Expert1(4×4特征) → [B, 512]
    ├─ Expert2(8×8特征) → [B, 512]
    └─ Expert3(16×16特征) → [B, 512]
    ↓
固定权重融合（不使用门控网络）
    └─ weight_1 × Expert1 + weight_2 × Expert2 + weight_3 × Expert3
    └─ 权重固定：例如 [0.33, 0.33, 0.34] 或 [0.5, 0.3, 0.2]
    ↓
最终特征 [B, 512]
```

### 与动态权重的对比

| 特性 | 固定权重 | 动态权重（门控网络） |
|------|---------|-------------------|
| 权重计算 | 预设固定值 | 门控网络学习 |
| 权重变化 | 训练过程中不变 | 随输入动态调整 |
| 门控网络 | 不创建，不训练 | 创建并训练 |
| 参数量 | 较少（无门控网络） | 较多（有门控网络） |
| 稳定性 | 高（权重固定） | 中等（可能波动） |
| 灵活性 | 低（无法自适应） | 高（可自适应） |

---

## 🚀 Linux 命令行

### 方法 1: 使用命令行参数（推荐）

#### 基本命令（均等权重）

```bash
cd /home/zhanghaoyang/Desktop/yzy/AboutReid

eval "$(conda shell.bash hook)"
conda activate MambaPro

python train_net.py \
    --config_file configs/RGBNT201/yzy_best_Mambapro_moe.yml \
    --use_moe \
    MODEL.USE_CLIP_MULTI_SCALE True \
    MODEL.CLIP_MULTI_SCALE_SCALES "[4,8,16]" \
    MODEL.MOE_USE_FIXED_WEIGHTS True \
    MODEL.MOE_FIXED_WEIGHTS "[0.33,0.33,0.34]" \
    OUTPUT_DIR "outputs/ablation/multiscale_fixed_weights_$(date +%Y%m%d_%H%M%S)"
```

#### 自定义权重（例如：强调 4×4 尺度）

```bash
cd /home/zhanghaoyang/Desktop/yzy/AboutReid

eval "$(conda shell.bash hook)"
conda activate MambaPro

python train_net.py \
    --config_file configs/RGBNT201/yzy_best_Mambapro_moe.yml \
    --use_moe \
    MODEL.USE_CLIP_MULTI_SCALE True \
    MODEL.CLIP_MULTI_SCALE_SCALES "[4,8,16]" \
    MODEL.MOE_USE_FIXED_WEIGHTS True \
    MODEL.MOE_FIXED_WEIGHTS "[0.5,0.3,0.2]" \
    OUTPUT_DIR "outputs/ablation/multiscale_fixed_weights_4x4_heavy_$(date +%Y%m%d_%H%M%S)"
```

#### 强调 16×16 尺度

```bash
cd /home/zhanghaoyang/Desktop/yzy/AboutReid

eval "$(conda shell.bash hook)"
conda activate MambaPro

python train_net.py \
    --config_file configs/RGBNT201/yzy_best_Mambapro_moe.yml \
    --use_moe \
    MODEL.USE_CLIP_MULTI_SCALE True \
    MODEL.CLIP_MULTI_SCALE_SCALES "[4,8,16]" \
    MODEL.MOE_USE_FIXED_WEIGHTS True \
    MODEL.MOE_FIXED_WEIGHTS "[0.2,0.3,0.5]" \
    OUTPUT_DIR "outputs/ablation/multiscale_fixed_weights_16x16_heavy_$(date +%Y%m%d_%H%M%S)"
```

---

### 方法 2: 使用配置文件

#### 步骤 1: 创建配置文件

创建新配置文件：`configs/RGBNT201/multiscale_fixed_weights.yml`

```yaml
MODEL:
  PRETRAIN_PATH_T: '/home/zhanghaoyang/Desktop/yzy/AboutReid/pths/ViT-B-16.pt'
  TRANSFORMER_TYPE: 'ViT-B-16'
  STRIDE_SIZE: [ 16, 16 ]
  SIE_CAMERA: True
  DIRECT: 1
  SIE_COE: 1.0
  ID_LOSS_WEIGHT: 0.25
  TRIPLET_LOSS_WEIGHT: 1.0
  PROMPT: True
  ADAPTER: True
  MAMBA: True
  FROZEN: True
  
  # ========== 多尺度滑动窗口配置：启用多尺度特征提取 ==========
  USE_CLIP_MULTI_SCALE: True   # ✅ 启用多尺度滑动窗口
  CLIP_MULTI_SCALE_SCALES: [4, 8, 16]  # ✅ 使用 4×4、8×8、16×16 三个尺度
  
  # ========== MoE配置：启用MOE，但使用固定权重 ==========
  USE_MULTI_SCALE_MOE: True    # ✅ 启用 MOE 专家网络
  MOE_SCALES: [4, 8, 16]       # ✅ MoE滑动窗口尺度（与多尺度滑动窗口保持一致）
  
  # ========== 固定权重配置：禁用门控网络，使用固定权重 ==========
  MOE_USE_FIXED_WEIGHTS: True  # ✅ 启用固定权重模式（禁用门控网络）
  MOE_FIXED_WEIGHTS: [0.33, 0.33, 0.34]  # ✅ 固定权重值（三个专家均等权重）
  # 注意：
  #   - 权重会自动归一化，无需手动确保和为1.0
  #   - 例如：[0.5, 0.5, 0.5] 会被归一化为 [0.33, 0.33, 0.34]
  #   - 权重数量必须等于专家数量（3个）
  
  # ========== MoE专家网络参数 ==========
  MOE_EXPERT_HIDDEN_DIM: 1024   # 专家网络隐藏层维度
  MOE_TEMPERATURE: 1.0          # 温度参数（固定权重模式下不使用）
  MOE_EXPERT_DROPOUT: 0.1       # 专家网络Dropout
  MOE_GATE_DROPOUT: 0.1         # 门控网络Dropout（固定权重模式下不使用）
  MOE_EXPERT_LAYERS: 2          # 专家网络层数
  MOE_GATE_LAYERS: 2            # 门控网络层数（固定权重模式下不使用）
  MOE_EXPERT_THRESHOLD: 0.1     # 专家激活阈值
  MOE_RESIDUAL_WEIGHT: 1.0      # 残差连接权重
  
  # ========== MoE损失权重（固定权重模式下建议降低或禁用） ==========
  # 注意：固定权重模式下，门控网络不参与训练，相关损失可能无意义
  # 建议：降低或禁用平衡损失、稀疏性损失等
  MOE_BALANCE_LOSS_WEIGHT: 0.0  # 平衡损失权重（建议设为0.0）
  MOE_SPARSITY_LOSS_WEIGHT: 0.0 # 稀疏性损失权重（建议设为0.0）
  MOE_DIVERSITY_LOSS_WEIGHT: 0.0 # 多样性损失权重（建议设为0.0）

INPUT:
  SIZE_TRAIN: [ 256, 128 ]
  SIZE_TEST: [ 256, 128 ]
  PROB: 0.5
  RE: 0.5
  PIXEL_MEAN: [0.485, 0.456, 0.406]
  PIXEL_STD: [0.229, 0.224, 0.225]
  PADDING: 10

DATASETS:
  NAMES: ('RGBNT201',)
  ROOT_DIR: ('/home/zhanghaoyang/Desktop/yzy/AboutReid/datasets',)

SOLVER:
  OPTIMIZER_NAME: 'AdamW'
  MAX_EPOCHS: 60
  BASE_LR: 0.00035
  WEIGHT_DECAY: 0.0005
  BIAS_LR_FACTOR: 1.0
  WEIGHT_DECAY_BIAS: 0.0005
  MOMENTUM: 0.9
  SCHEDULER: 'cosine'
  WARMUP_EPOCHS: 5
  WARMUP_FACTOR: 0.01
  WARMUP_METHOD: 'linear'
  WARMUP_ITERS: 500
  FREEZE_EPOCHS: 0
  GAMMA: 0.1
  STEPSIZE: [20, 40]
  LR_SCHEDULER: 'cosine'
  ETA_MIN_LR: 0.0

TEST:
  IMS_PER_BATCH: 128
  WEIGHT: ''
  EVAL_PERIOD: 5

OUTPUT_DIR: 'outputs/ablation/multiscale_fixed_weights'
```

#### 步骤 2: 运行训练

```bash
cd /home/zhanghaoyang/Desktop/yzy/AboutReid

eval "$(conda shell.bash hook)"
conda activate MambaPro

python train_net.py \
    --config_file configs/RGBNT201/multiscale_fixed_weights.yml
```

---

## 📊 常用权重配置示例

### 1. 均等权重（Baseline）

```bash
MODEL.MOE_FIXED_WEIGHTS "[0.33,0.33,0.34]"
# 或
MODEL.MOE_FIXED_WEIGHTS "[0.333,0.333,0.334]"
```

**说明**：三个专家权重相等，适合作为基线对比。

---

### 2. 强调细粒度（4×4 主导）

```bash
MODEL.MOE_FIXED_WEIGHTS "[0.5,0.3,0.2]"
# 或更极端
MODEL.MOE_FIXED_WEIGHTS "[0.7,0.2,0.1]"
```

**说明**：强调细粒度特征（4×4），适合需要精细细节的任务。

---

### 3. 强调粗粒度（16×16 主导）

```bash
MODEL.MOE_FIXED_WEIGHTS "[0.2,0.3,0.5]"
# 或更极端
MODEL.MOE_FIXED_WEIGHTS "[0.1,0.2,0.7]"
```

**说明**：强调粗粒度特征（16×16），适合需要全局信息的任务。

---

### 4. 强调中等粒度（8×8 主导）

```bash
MODEL.MOE_FIXED_WEIGHTS "[0.2,0.6,0.2]"
```

**说明**：强调中等粒度特征（8×8），平衡细粒度和粗粒度。

---

### 5. 渐进式权重（从细到粗递减）

```bash
MODEL.MOE_FIXED_WEIGHTS "[0.5,0.3,0.2]"
```

**说明**：细粒度权重最大，粗粒度权重最小。

---

### 6. 渐进式权重（从粗到细递减）

```bash
MODEL.MOE_FIXED_WEIGHTS "[0.2,0.3,0.5]"
```

**说明**：粗粒度权重最大，细粒度权重最小。

---

## 🔍 验证配置

训练开始后，检查日志文件，确认以下配置：

```
✅ USE_CLIP_MULTI_SCALE: True
✅ CLIP_MULTI_SCALE_SCALES: [4, 8, 16]
✅ USE_MULTI_SCALE_MOE: True
✅ MOE_USE_FIXED_WEIGHTS: True
✅ MOE_FIXED_WEIGHTS: [0.33, 0.33, 0.34] (或您设置的值)
```

### 检查命令

```bash
# 查看最新日志文件
tail -f outputs/ablation/multiscale_fixed_weights_*/logs/train_*.log | grep -E "USE_CLIP_MULTI_SCALE|USE_MULTI_SCALE_MOE|MOE_USE_FIXED_WEIGHTS|MOE_FIXED_WEIGHTS"
```

---

## 📊 实验对比

### 实验设置对比

| 配置项 | 多尺度 + 动态权重 | 多尺度 + 固定权重 |
|--------|-----------------|-----------------|
| `USE_CLIP_MULTI_SCALE` | ✅ True | ✅ True |
| `CLIP_MULTI_SCALE_SCALES` | [4, 8, 16] | [4, 8, 16] |
| `USE_MULTI_SCALE_MOE` | ✅ True | ✅ True |
| `MOE_USE_FIXED_WEIGHTS` | ❌ False | ✅ True |
| 门控网络 | ✅ 创建并训练 | ❌ 不创建 |
| 权重计算 | 动态（门控网络学习） | 固定（预设值） |
| 参数量 | 较多 | 较少 |
| 稳定性 | 中等 | 高 |

---

## 🚀 快速启动脚本

创建脚本文件：`run_multiscale_fixed_weights.sh`

```bash
#!/bin/bash

# 多尺度（4、8、16）+ 固定权重实验启动脚本
# 功能：使用多尺度滑动窗口提取特征，MOE专家网络处理，但使用固定权重融合

cd /home/zhanghaoyang/Desktop/yzy/AboutReid

eval "$(conda shell.bash hook)"
conda activate MambaPro

# 默认权重：均等权重
WEIGHTS="${1:-[0.33,0.33,0.34]}"

echo "🚀 开始训练：多尺度（4、8、16）+ 固定权重实验"
echo "配置："
echo "  ✅ USE_CLIP_MULTI_SCALE: True"
echo "  ✅ CLIP_MULTI_SCALE_SCALES: [4, 8, 16]"
echo "  ✅ USE_MULTI_SCALE_MOE: True"
echo "  ✅ MOE_USE_FIXED_WEIGHTS: True"
echo "  ✅ MOE_FIXED_WEIGHTS: $WEIGHTS"
echo ""

python train_net.py \
    --config_file configs/RGBNT201/yzy_best_Mambapro_moe.yml \
    --use_moe \
    MODEL.USE_CLIP_MULTI_SCALE True \
    MODEL.CLIP_MULTI_SCALE_SCALES "[4,8,16]" \
    MODEL.MOE_USE_FIXED_WEIGHTS True \
    MODEL.MOE_FIXED_WEIGHTS "$WEIGHTS" \
    OUTPUT_DIR "outputs/ablation/multiscale_fixed_weights_$(date +%Y%m%d_%H%M%S)"

echo ""
echo "✅ 训练完成！"
```

### 使用脚本

```bash
# 使用默认权重（均等权重）
chmod +x run_multiscale_fixed_weights.sh
./run_multiscale_fixed_weights.sh

# 使用自定义权重（强调 4×4）
./run_multiscale_fixed_weights.sh "[0.5,0.3,0.2]"

# 使用自定义权重（强调 16×16）
./run_multiscale_fixed_weights.sh "[0.2,0.3,0.5]"
```

---

## 📝 注意事项

1. **权重归一化**：系统会自动归一化权重，无需手动确保和为1.0
   - 例如：`[0.5, 0.5, 0.5]` 会被归一化为 `[0.33, 0.33, 0.34]`

2. **权重数量**：权重数量必须等于专家数量（3个）
   - 如果权重数量不匹配，系统会使用均等权重并发出警告

3. **门控网络**：固定权重模式下，门控网络不会被创建，不参与训练
   - 相关参数（如 `MOE_GATE_DROPOUT`、`MOE_GATE_LAYERS`）不会生效

4. **MoE 损失**：固定权重模式下，建议降低或禁用 MoE 相关损失
   - `MOE_BALANCE_LOSS_WEIGHT: 0.0`
   - `MOE_SPARSITY_LOSS_WEIGHT: 0.0`
   - `MOE_DIVERSITY_LOSS_WEIGHT: 0.0`

5. **专家网络**：专家网络仍然参与训练，只是权重固定

---

## 🎯 适用场景

1. **消融实验**：对比固定权重 vs 动态权重的效果差异
2. **稳定性测试**：固定权重更稳定，适合跨域鲁棒性测试
3. **性能分析**：排除门控网络影响，专注于专家网络性能
4. **权重敏感性分析**：测试不同权重配置对性能的影响

---

## 🔗 相关文档

- **消融实验方案**: `消融实验_MOE替代方案.md`
- **多尺度配置说明**: `Readme合集/8-多尺度配置说明.md`
- **MoE 命令行开关**: `Readme合集/32-MoE命令行开关使用说明.md`
- **多尺度无 MOE**: `实验命令_多尺度无MOE.md`

---

**最后更新**: 2025-12-21
