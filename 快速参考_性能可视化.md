# 性能与专家权重可视化 - 快速参考

## 🚀 快速命令

### 基本使用
```bash
cd /home/zhanghaoyang/Desktop/yzy/AboutReid
eval "$(conda shell.bash hook)"
conda activate MambaPro

python visualize_performance_expert_weights.py \
    --log_path /path/to/train_*.log
```

### 指定输出路径
```bash
python visualize_performance_expert_weights.py \
    --log_path /path/to/train_*.log \
    --output_path outputs/performance_analysis/model_name.png \
    --title_suffix " - Model Name"
```

---

## 📊 指标含义速查

| 指标 | 含义 | 范围 | 越高越好 |
|------|------|------|---------|
| **mAP** | 平均精度均值，综合评估模型识别性能 | 0-100% | ✅ |
| **Rank-1** | 首位命中率，第一位检索结果正确的比例 | 0-100% | ✅ |
| **Scale 4×4 权重** | 细粒度专家的权重占比 | 0.0-1.0 | 取决于任务 |
| **Scale 8×8 权重** | 中等粒度专家的权重占比 | 0.0-1.0 | 取决于任务 |
| **Scale 16×16 权重** | 粗粒度专家的权重占比 | 0.0-1.0 | 取决于任务 |

---

## 🔍 查找日志文件

```bash
# 查找所有训练日志
find /home/zhanghaoyang/Desktop/yzy -name "train_*.log" -type f

# 查找特定模型的日志
find /path/to/model -name "train_*.log" -type f
```

---

## 📈 常见场景

### 场景1: 单个模型
```bash
python visualize_performance_expert_weights.py \
    --log_path /path/to/model/logs/train_*.log \
    --title_suffix " - Model Name"
```

### 场景2: multiscale 所有模型
```bash
for model_dir in outputs/multiscale/*/; do
    log_file=$(find "$model_dir" -name "train_*.log" | head -1)
    model_name=$(basename "$model_dir")
    python visualize_performance_expert_weights.py \
        --log_path "$log_file" \
        --title_suffix " - $model_name" \
        --output_path "outputs/performance_analysis/${model_name}.png"
done
```

---

## 📁 输出位置

- **默认**：日志文件同目录下的 `performance_expert_weights.png`
- **指定**：`--output_path` 参数指定的路径

---

## ✅ 检查清单

- [ ] conda 环境已激活
- [ ] 日志文件存在
- [ ] 日志文件包含验证结果
- [ ] 输出目录有写入权限

---

**详细文档**：`README_性能与专家权重可视化使用指南.md`
