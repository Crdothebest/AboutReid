# 第2章 &emsp;相关理论概述

&emsp;&emsp;本章系统介绍支撑本文两项创新方法的核心理论基础。第2.1节给出跨模态行人重识别的任务形式化定义与评估体系；第2.2节介绍本文视觉骨干所依赖的CLIP视觉-语言预训练模型及其ViT视觉编码器架构；第2.3节阐述Mamba状态空间模型的工作机制及其在多模态融合中的应用原理；第2.4节介绍混合专家网络（MoE）的条件计算机制，为第3章的多尺度融合设计提供理论支撑；第2.5节介绍本文训练阶段所采用的度量学习损失函数体系，包括身份分类损失、三元组损失及MoE辅助损失。

---

## 2.1&emsp;跨模态行人重识别基础

### 2.1.1&emsp;任务定义

&emsp;&emsp;**行人重识别**（Person Re-Identification，ReID）是一类细粒度实例检索任务，其目标是：给定一张来自查询摄像头的行人图像（Query），在由不同摄像头或不同时刻采集的候选图像库（Gallery）中，找到与查询图像属于同一身份的全部候选图像，并按相似度排序返回检索结果。

&emsp;&emsp;**跨模态行人重识别**（Cross-Modal Person Re-Identification）在上述基础上进一步引入了模态异构性：查询集与图库集中的图像可能来自不同的成像传感器，如可见光（RGB）、近红外（NIR）或热红外（TIR）摄像头。由于各模态的物理成像机制存在本质差异，同一行人在不同模态下的外观呈现出显著的视觉差异，这大幅增加了特征匹配的难度。

&emsp;&emsp;**形式化定义。** 设训练集为 $\mathcal{D}_{\text{train}} = \{(x_i^m, y_i)\}$，其中 $x_i^m$ 表示第 $i$ 个样本在模态 $m \in \mathcal{M} = \{\text{RGB, NIR, TIR}\}$ 下的图像，$y_i \in \{1, 2, \ldots, C\}$ 为对应的行人身份标签，$C$ 为训练集中的身份总数。测试阶段，给定查询集 $\mathcal{Q}$ 和图库集 $\mathcal{G}$，跨模态行人重识别的目标是训练一个特征提取函数 $\phi(\cdot)$，使得同一身份的跨模态特征相似度高于不同身份：

$$\forall\, y_i = y_j \neq y_k:\quad \text{sim}\bigl(\phi(x_i^{m_1}),\, \phi(x_j^{m_2})\bigr) > \text{sim}\bigl(\phi(x_i^{m_1}),\, \phi(x_k^{m_3})\bigr)$$

其中 $m_1, m_2, m_3 \in \mathcal{M}$ 可以相同也可以不同，$\text{sim}(\cdot, \cdot)$ 表示余弦相似度或负欧氏距离。

### 2.1.2&emsp;主要挑战

&emsp;&emsp;多模态行人重识别面临的核心挑战可以归纳为以下四个方面：

**（1）模态内差异（Intra-Modal Variation）。** 即使在同一模态内部，同一行人在不同摄像头视角、不同光照条件、不同姿态或存在遮挡时，其外观特征仍会发生显著变化。这是所有行人重识别任务共同面临的基础性挑战。

**（2）模态间差异（Inter-Modal Discrepancy）。** 不同成像模态对目标的感知维度本质不同：RGB 图像捕获波长 400—700 nm 的可见光，其感知量是目标表面的颜色与纹理反射；NIR 图像感知波长 700—1400 nm 的近红外反射，主要反映材质的反射特性，对颜色不敏感；TIR 图像感知波长 8—14 μm 的热辐射，反映目标表面温度分布，外观与可见光迥异。这种成像机制的根本差异导致同一行人的跨模态外观距离可能大于不同行人在同一模态内的外观距离，是跨模态匹配最根本的难点。

**（3）长尾身份分布（Long-tail Identity Distribution）。** 实际监控数据集中，不同行人的图像数量往往分布极不均匀，频繁出现的身份图像数量远多于稀少身份，这为模型的均衡学习带来困难。

**（4）领域差距（Domain Gap）。** 不同数据集采集环境（室内/室外、昼间/夜间、不同摄像头型号）导致的特征分布偏移，使得模型在跨数据集条件下的泛化能力受限。

### 2.1.3&emsp;评估指标

&emsp;&emsp;行人重识别任务的性能评估主要采用以下两类标准指标。

**（1）累积匹配特征曲线（Cumulative Matching Characteristics，CMC）**

&emsp;&emsp;CMC 曲线描述了在检索结果的前 $k$ 位中包含至少一张正确匹配图像的概率，即 Rank-$k$ 准确率：

$$\text{Rank-}k = \frac{1}{|\mathcal{Q}|} \sum_{q \in \mathcal{Q}} \mathbf{1}\bigl[\text{rank}(q) \leq k\bigr]$$

其中 $\text{rank}(q)$ 表示查询 $q$ 的第一个正确匹配在排序结果中的位置。实践中常报告 Rank-1、Rank-5 和 Rank-10 三个关键值，其中 **Rank-1** 是最重要的单点指标，反映系统精确定位目标的能力。

**（2）平均精度均值（Mean Average Precision，mAP）**

&emsp;&emsp;对于每个查询 $q$，其平均精度（AP）综合考虑了所有相关图像的检索位置：

$$\text{AP}(q) = \frac{1}{N_q^+} \sum_{k=1}^{|\mathcal{G}|} P@k \cdot \text{rel}(k)$$

其中 $N_q^+$ 为图库中与查询 $q$ 同一身份的图像总数，$P@k$ 为前 $k$ 位结果的精确率，$\text{rel}(k) \in \{0,1\}$ 指示第 $k$ 位检索结果是否与查询同一身份。mAP 为所有查询的 AP 均值：

$$\text{mAP} = \frac{1}{|\mathcal{Q}|} \sum_{q \in \mathcal{Q}} \text{AP}(q)$$

&emsp;&emsp;相较于 Rank-1，mAP 对检索列表中**全部**正确匹配的位置均有敏感性，是衡量系统整体检索质量的更全面指标，也是本文实验中最核心的评估依据。

---

## 2.2&emsp;CLIP视觉-语言预训练模型

### 2.2.1&emsp;对比语言-图像预训练原理

&emsp;&emsp;CLIP（Contrastive Language-Image Pre-training）[1] 是由 OpenAI 于 2021 年提出的视觉-语言预训练框架。其核心思路是：在包含 4 亿图文对的大规模数据集上，通过**对比学习**目标同时训练视觉编码器 $f_v(\cdot)$ 和文本编码器 $f_t(\cdot)$，使匹配的图文对在联合嵌入空间中的余弦相似度最大化，同时使不匹配的图文对相似度最小化。

&emsp;&emsp;对于批次大小为 $N$ 的图文对 $\{(I_i, T_i)\}_{i=1}^N$，CLIP 的对称对比损失为：

$$\mathcal{L}_{\text{CLIP}} = -\frac{1}{2N} \sum_{i=1}^{N} \left[ \log \frac{e^{\text{sim}(I_i, T_i)/\tau}}{\sum_{j=1}^{N} e^{\text{sim}(I_i, T_j)/\tau}} + \log \frac{e^{\text{sim}(T_i, I_i)/\tau}}{\sum_{j=1}^{N} e^{\text{sim}(T_j, I_i)/\tau}} \right]$$

其中 $\text{sim}(I_i, T_j) = f_v(I_i)^\top f_t(T_j) / (\|f_v(I_i)\| \cdot \|f_t(T_j)\|)$ 为归一化后的余弦相似度，$\tau$ 为可学习的温度参数。

&emsp;&emsp;经过大规模预训练后，CLIP 建立了一个**模态无关的统一语义空间**：语义相近的图像与文本在该空间中距离相近，这为后续各类下游任务的零样本和少样本迁移奠定了基础。本文利用 CLIP ViT-B/16 作为视觉骨干，其预训练权重提供了强大的视觉语义先验；同时利用 CLIP 文本编码器为三种成像模态生成语义先验特征，实现文本语义对视觉特征的引导增强。

### 2.2.2&emsp;Vision Transformer视觉编码器

&emsp;&emsp;CLIP 的视觉编码器采用 Vision Transformer（ViT）[2] 架构。ViT 将传统 CNN 的卷积滑动窗口替换为基于自注意力的序列建模，其核心处理流程如下。

**（1）图像分块与线性嵌入**

&emsp;&emsp;对于输入图像 $I \in \mathbb{R}^{H \times W \times 3}$，ViT 首先将其切分为 $N = \frac{H}{P} \times \frac{W}{P}$ 个不重叠的图像块（Patch），每个 Patch 的尺寸为 $P \times P \times 3$。本文使用的 ViT-B/16 中 $P=16$，对于 $256 \times 128$ 的输入图像，产生 $N = 16 \times 8 = 128$ 个 Patch。每个 Patch 经线性投影层映射为 $D=512$ 维的嵌入向量：

$$\mathbf{z}_i^{(0)} = \mathbf{W}_E \cdot \text{vec}(P_i) + \mathbf{b}_E, \quad i = 1, 2, \ldots, N$$

其中 $\mathbf{W}_E \in \mathbb{R}^{D \times (P^2 \times 3)}$ 为可学习投影矩阵。

**（2）CLS Token与位置编码**

&emsp;&emsp;在 Patch 序列前端拼接一个可学习的分类标记（CLS Token）$\mathbf{z}_{\text{cls}}^{(0)} \in \mathbb{R}^D$，随后与可学习的一维位置编码 $\mathbf{E}_{\text{pos}} \in \mathbb{R}^{(N+1) \times D}$ 相加，得到 Transformer 输入序列：

$$\mathbf{Z}^{(0)} = \bigl[\mathbf{z}_{\text{cls}}^{(0)},\; \mathbf{z}_1^{(0)},\; \mathbf{z}_2^{(0)},\; \ldots,\; \mathbf{z}_N^{(0)}\bigr] + \mathbf{E}_{\text{pos}} \in \mathbb{R}^{(N+1) \times D}$$

**（3）Transformer编码器**

&emsp;&emsp;输入序列经 $L$ 层 Transformer Block 逐层处理，每层由多头自注意力（Multi-Head Self-Attention，MSA）和前馈网络（Feed-Forward Network，FFN）两个子模块构成，均采用残差连接和层归一化（Layer Normalization，LN）：

$$\tilde{\mathbf{Z}}^{(l)} = \text{MSA}\!\left(\text{LN}(\mathbf{Z}^{(l-1)})\right) + \mathbf{Z}^{(l-1)}$$

$$\mathbf{Z}^{(l)} = \text{FFN}\!\left(\text{LN}(\tilde{\mathbf{Z}}^{(l)})\right) + \tilde{\mathbf{Z}}^{(l)}$$

其中多头自注意力机制的计算为：

$$\text{MSA}(\mathbf{Z}) = \text{Concat}\!\left(\text{head}_1, \ldots, \text{head}_h\right) \mathbf{W}^O$$

$$\text{head}_k = \text{Attention}\!\left(\mathbf{Z}\mathbf{W}_k^Q,\; \mathbf{Z}\mathbf{W}_k^K,\; \mathbf{Z}\mathbf{W}_k^V\right) = \text{Softmax}\!\left(\frac{\mathbf{Z}\mathbf{W}_k^Q (\mathbf{Z}\mathbf{W}_k^K)^\top}{\sqrt{d_k}}\right)\mathbf{Z}\mathbf{W}_k^V$$

ViT-B/16 共有 $L=12$ 层，12 个注意力头，隐层维度 $D=768$，经最终投影层降至 $D=512$。

**（4）特征输出**

&emsp;&emsp;经 $L$ 层编码后，本文提取两类特征用于后续处理：
- **CLS Token** $\mathbf{z}_{\text{cls}}^{(L)} \in \mathbb{R}^{D}$：全局图像表征，编码了整张图像的语义信息；
- **Patch Token 序列** $\{\mathbf{z}_i^{(L)}\}_{i=1}^N \in \mathbb{R}^{N \times D}$：各局部区域的空间细粒度特征，是本文多尺度滑动窗口机制的输入来源。

### 2.2.3&emsp;CLIP文本编码器

&emsp;&emsp;CLIP 的文本编码器是一个标准的 Transformer 语言模型，将文本描述 $T$ 编码为与视觉特征处于同一语义空间的文本嵌入向量 $\mathbf{t} = f_t(T) \in \mathbb{R}^{D_t}$，其中 $D_t = 512$。

&emsp;&emsp;文本编码的流程为：输入文本首先经 BPE（Byte Pair Encoding）分词，转化为词元序列并添加起始符 [SOS] 和终止符 [EOS]；序列经词嵌入和位置编码后送入 12 层 Transformer Block；最终提取 [EOS] 位置的隐层输出并经线性投影得到文本特征 $\mathbf{t}$。

&emsp;&emsp;由于 CLIP 视觉编码器和文本编码器均在同一对比学习目标下联合训练，两者的输出特征处于对齐的联合语义空间，无需额外的跨模态对齐训练即可进行直接的视文特征交互。本文在第4章提出的文本语义引导机制正是基于这一特性构建的。

---

## 2.3&emsp;Mamba状态空间模型

### 2.3.1&emsp;状态空间模型基础

&emsp;&emsp;状态空间模型（State Space Model，SSM）起源于控制理论，描述线性时不变系统的动态行为。经典连续时间 SSM 将输入序列 $u(t)$ 映射到输出序列 $y(t)$，过程由以下方程刻画：

$$\dot{\mathbf{h}}(t) = \mathbf{A}\mathbf{h}(t) + \mathbf{B}u(t)$$

$$y(t) = \mathbf{C}\mathbf{h}(t) + \mathbf{D}u(t)$$

其中 $\mathbf{h}(t) \in \mathbb{R}^N$ 为隐状态，$\mathbf{A} \in \mathbb{R}^{N \times N}$ 为状态转移矩阵，$\mathbf{B} \in \mathbb{R}^{N \times 1}$、$\mathbf{C} \in \mathbb{R}^{1 \times N}$ 分别为输入投影矩阵和输出投影矩阵，$\mathbf{D}$ 为跳跃连接系数。

&emsp;&emsp;为适配离散序列数据，需对连续 SSM 进行零阶保持（Zero-Order Hold，ZOH）离散化，步长参数为 $\Delta$：

$$\bar{\mathbf{A}} = e^{\Delta \mathbf{A}}, \quad \bar{\mathbf{B}} = (\Delta \mathbf{A})^{-1}(e^{\Delta \mathbf{A}} - \mathbf{I}) \cdot \Delta \mathbf{B}$$

离散化后的递推公式为：

$$\mathbf{h}_t = \bar{\mathbf{A}}\mathbf{h}_{t-1} + \bar{\mathbf{B}}u_t, \quad y_t = \mathbf{C}\mathbf{h}_t + \mathbf{D}u_t$$

### 2.3.2&emsp;Mamba的选择性扫描机制

&emsp;&emsp;传统 SSM（如 S4 [3]）中的参数矩阵 $\mathbf{A}, \mathbf{B}, \mathbf{C}$ 对所有输入位置保持不变，这导致模型对序列中各位置的内容无选择性，难以灵活处理信息密度不均匀的序列。Gu 和 Dao [4] 提出的 **Mamba** 模型引入了**选择性扫描**（Selective Scan）机制，使参数成为输入的函数：

$$\mathbf{B}_t = s_B(u_t), \quad \mathbf{C}_t = s_C(u_t), \quad \Delta_t = \text{Softplus}(s_\Delta(u_t))$$

其中 $s_B(\cdot), s_C(\cdot), s_\Delta(\cdot)$ 均为可学习的线性投影。通过令参数随输入动态变化，Mamba 具备了根据当前输入内容选择性地保留或遗忘历史状态信息的能力，在长序列建模上显著优于固定参数的 SSM。

&emsp;&emsp;Mamba 的另一重要特性是其**线性计算复杂度**。相较于 Transformer 自注意力的 $O(N^2)$ 复杂度，Mamba 的选择性扫描可以高效地以 $O(N)$ 复杂度处理长度为 $N$ 的序列，并通过并行扫描算法实现 GPU 上的高效计算。这一特性使 Mamba 非常适合处理高分辨率图像的 Patch Token 序列或跨摄像头的长时序监控视频流。

### 2.3.3&emsp;MM-SS2D：多模态二维选择性扫描

&emsp;&emsp;本文所基于的 MambaPro 框架 [5] 针对多模态行人重识别任务，设计了多模态二维选择性扫描模块（Multi-Modal 2D Selective Scan，MM-SS2D）。该模块在经典 Mamba 的一维序列建模基础上进行了双维度扩展：

- **模态内扫描（Intra-Modal Scan）**：在同一模态的 Patch Token 序列内，利用 Mamba 捕获行人图像的空间局部依赖关系；
- **模态间扫描（Inter-Modal Scan）**：跨 RGB、NIR、TIR 三个模态进行联合序列建模，捕获不同模态在同一空间位置上的互补语义依赖。

&emsp;&emsp;聚合注意力模块（Aggregation Attention Module，AAM）将三路模态的 Patch Token 分别送入多层 MM-SS2D 进行联合建模，随后对每路模态的 CLS Token 与空间均值 Token 进行拼接，通过线性降维得到各模态的紧凑全局特征：

$$r_{\text{feat}} = W_r \cdot [\text{cls}_r \;\|\; \overline{p}_r], \quad n_{\text{feat}} = W_n \cdot [\text{cls}_n \;\|\; \overline{p}_n], \quad t_{\text{feat}} = W_t \cdot [\text{cls}_t \;\|\; \overline{p}_t]$$

$$\mathbf{f}_v = [r_{\text{feat}} \;\|\; n_{\text{feat}} \;\|\; t_{\text{feat}}] \in \mathbb{R}^{3D}$$

其中 $\overline{p}_m = \frac{1}{N}\sum_{i=1}^N \mathbf{z}_i^{(L),m}$ 为第 $m$ 模态 Patch Token 的空间均值，$W_m \in \mathbb{R}^{D \times 2D}$ 为各模态独立的线性降维矩阵，$D=512$，最终拼接特征 $\mathbf{f}_v \in \mathbb{R}^{1536}$。

---

## 2.4&emsp;混合专家网络

### 2.4.1&emsp;条件计算与混合专家网络原理

&emsp;&emsp;混合专家网络（Mixture-of-Experts，MoE）[6] 的核心思想是：对于不同类型或不同特征的输入，激活模型中专门化的子网络（Expert）进行处理，而非对所有输入执行相同的计算路径。这种**条件计算**（Conditional Computation）机制能够在保持计算量可控的前提下大幅提升模型的表达容量。

&emsp;&emsp;标准 MoE 层由 $K$ 个专家子网络 $\{E_k\}_{k=1}^K$ 和一个门控网络 $G(\cdot)$ 组成，其输出为：

$$\mathbf{y} = \sum_{k=1}^{K} G(\mathbf{x})_k \cdot E_k(\mathbf{x})$$

其中 $G(\mathbf{x}) \in \mathbb{R}^K$ 为门控网络输出的专家权重向量，满足 $\sum_{k=1}^K G(\mathbf{x})_k = 1$，$G(\mathbf{x})_k \geq 0$。

### 2.4.2&emsp;门控网络设计

&emsp;&emsp;门控网络 $G(\cdot)$ 负责根据输入特征的内容动态计算各专家的权重分配。本文采用的密集门控网络（Dense Gating）为所有专家分配非零权重，适合专家数目较少（$K=3$）的场景：

$$G(\mathbf{x}) = \text{Softmax}\!\left(\frac{g(\mathbf{x})}{\tau}\right), \quad g(\mathbf{x}) = \mathbf{W}_2 \cdot \text{GELU}\!\left(\text{LN}\!\left(\mathbf{W}_1 \cdot \mathbf{x}\right)\right)$$

其中 $\mathbf{W}_1, \mathbf{W}_2$ 为可学习参数，$\tau$ 为温度超参数（温度越低，权重分布越尖锐，即越接近"赢者通吃"的稀疏选择）。引入温度参数使训练过程中可以通过调整 $\tau$ 来平衡专家的使用均衡性。

### 2.4.3&emsp;专家网络结构

&emsp;&emsp;本文中每个专家网络 $E_k$ 是一个带残差连接的两层前馈网络（Feed-Forward Network，FFN），其结构遵循标准 Transformer FFN 设计：

$$E_k(\mathbf{x}) = \mathbf{W}_k^{(2)} \cdot \text{GELU}\!\left(\mathbf{W}_k^{(1)} \cdot \mathbf{x}\right) + \mathbf{x}$$

其中 $\mathbf{W}_k^{(1)} \in \mathbb{R}^{D_h \times D}$，$\mathbf{W}_k^{(2)} \in \mathbb{R}^{D \times D_h}$，$D_h$ 为隐层维度（本文默认 $D_h = 1024$），残差连接保留输入的原始信息并缓解梯度消失问题。各专家之间的参数完全独立，使得每个专家能够针对其对应输入类型学习专门化的变换。

### 2.4.4&emsp;MoE辅助损失

&emsp;&emsp;在训练过程中，MoE 容易出现**专家塌缩**（Expert Collapse）问题：门控网络倾向于固定地将大量输入路由至同一个专家，导致其他专家因梯度稀少而退化为无效分支。为此，本文引入三项辅助损失以维持专家分工的均衡性和多样性。

**（1）专家平衡损失（Balance Loss）**

$$\mathcal{L}_{\text{balance}} = \frac{1}{K} \sum_{k=1}^{K} \left[\text{ReLU}\!\left(\frac{|\bar{w}_k - \frac{1}{K}|}{\frac{1}{K}} - \delta\right)\right]^2$$

其中 $\bar{w}_k = \frac{1}{B}\sum_{b=1}^B G(\mathbf{x}_b)_k$ 为批次内第 $k$ 个专家的平均使用频率，$\delta$ 为容忍阈值（默认 $\delta=0.3$），该损失仅对相对偏差超过阈值 $\delta$ 的情形施加 L2 惩罚，避免过度约束正常的专家差异化使用。

**（2）专家稀疏性损失（Sparsity Loss）**

$$\mathcal{L}_{\text{sparsity}} = \frac{1}{B} \sum_{b=1}^{B} \frac{1 - \sum_{k=1}^K G(\mathbf{x}_b)_k^2}{1 - \frac{1}{K}}$$

该损失基于 Gini 不纯度原理：当权重完全稀疏时（仅一个专家权重为1，其余为0），$\sum_k w_k^2 = 1$，损失为0；当权重均匀分布时，$\sum_k w_k^2 = 1/K$，损失为1。通过最小化该损失，鼓励门控网络对每个输入做出更明确的专家选择。

**（3）专家多样性损失（Diversity Loss）**

$$\mathcal{L}_{\text{diversity}} = \frac{\sum_{i \neq j} \text{corr}(G(\cdot)_i, G(\cdot)_j)}{K(K-1)}$$

其中 $\text{corr}(\cdot, \cdot)$ 表示批次维度上两专家权重向量的余弦相关系数。该损失通过最小化不同专家权重向量之间的相关性，鼓励门控网络为不同输入激活不同的专家组合，维持专家分工的多样性。

**总辅助损失**为三项之和：

$$\mathcal{L}_{\text{MoE}} = \lambda_b \mathcal{L}_{\text{balance}} + \lambda_s \mathcal{L}_{\text{sparsity}} + \lambda_d \mathcal{L}_{\text{diversity}}$$

---

## 2.5&emsp;度量学习损失函数体系

### 2.5.1&emsp;身份分类损失

&emsp;&emsp;身份分类损失（ID Loss）将行人重识别视为闭集分类问题，对训练集中的每个行人身份设定一个类别，利用带标签平滑的交叉熵损失进行监督学习。

**标签平滑交叉熵（Label Smoothing Cross-Entropy）**

&emsp;&emsp;标准交叉熵损失存在过度自信问题，即模型容易将正确类别的预测概率驱向1，而将错误类别驱向0，导致特征嵌入边界过于尖锐，泛化能力下降。Szegedy 等人 [7] 提出的标签平滑技术通过软化目标分布缓解上述问题：

$$\tilde{y}_c = \begin{cases} 1 - \varepsilon + \dfrac{\varepsilon}{C} & c = y \\ \dfrac{\varepsilon}{C} & c \neq y \end{cases}$$

其中 $y$ 为真实类别标签，$C$ 为类别总数，$\varepsilon$ 为平滑系数（本文默认 $\varepsilon=0.1$）。标签平滑交叉熵损失为：

$$\mathcal{L}_{\text{ID}} = -\sum_{c=1}^{C} \tilde{y}_c \log p_c = -(1 - \varepsilon)\log p_y - \frac{\varepsilon}{C}\sum_{c=1}^{C} \log p_c$$

其中 $p_c = \text{Softmax}(\mathbf{W}_c^\top \mathbf{f})$ 为预测为第 $c$ 类的概率，$\mathbf{W}_c$ 为第 $c$ 个类别中心向量，$\mathbf{f}$ 为输入特征。

### 2.5.2&emsp;三元组损失

&emsp;&emsp;三元组损失（Triplet Loss）[8] 是行人重识别中最常用的度量学习目标，通过直接约束特征空间中同类样本的聚合与异类样本的分离来提升特征的判别性。

&emsp;&emsp;对于输入批次中的每个锚点样本 $\mathbf{f}_a$，分别找到与其同一身份的最难正样本 $\mathbf{f}_p^*$ 和最难负样本 $\mathbf{f}_n^*$（即最大正样本距离和最小负样本距离）：

$$\mathbf{f}_p^* = \arg\max_{\mathbf{f}_p: y_p = y_a} d(\mathbf{f}_a, \mathbf{f}_p), \quad \mathbf{f}_n^* = \arg\min_{\mathbf{f}_n: y_n \neq y_a} d(\mathbf{f}_a, \mathbf{f}_n)$$

三元组损失采用软间隔（Soft Margin）形式：

$$\mathcal{L}_{\text{triplet}} = \frac{1}{B} \sum_{a=1}^{B} \log\!\left(1 + e^{d(\mathbf{f}_a,\, \mathbf{f}_p^*) - d(\mathbf{f}_a,\, \mathbf{f}_n^*)}\right)$$

其中 $d(\cdot, \cdot) = \|\cdot - \cdot\|_2$ 为欧氏距离。软间隔形式相比硬间隔三元组损失（Hard Margin Triplet）不含固定裕量超参数，训练更为稳定，且在特征已被良好分离后梯度自然趋近于零，避免了过度优化问题。

&emsp;&emsp;本文中三元组损失基于批次内在线难样本挖掘（Batch Hard Mining）策略：在由随机身份采样策略（每批次采样 $P$ 个身份，每个身份采样 $K$ 张图像）构成的训练批次中，对批次内所有样本对进行两两距离计算，实时挖掘最难的正负样本对，保证三元组的信息量。

### 2.5.3&emsp;BNNeck归一化颈部

&emsp;&emsp;BNNeck [9] 是行人重识别中广泛采用的特征归一化结构。其核心思想是在特征嵌入后引入批归一化层（BatchNorm），使特征在超球面上分布更均匀，从而同时满足分类损失（使用 BatchNorm 后的归一化特征）和度量学习损失（使用 BatchNorm 前的原始特征）的不同需求：

- **归一化前特征** $\mathbf{f}$：用于三元组损失的距离度量，保留特征的幅度信息；
- **归一化后特征** $\mathbf{f}' = \text{BN}(\mathbf{f})$：用于分类损失的线性分类器，保证类别超平面的稳定性。

&emsp;&emsp;本文的整体训练损失为：

$$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{ID}} + \lambda_t \mathcal{L}_{\text{triplet}} + \lambda_m \mathcal{L}_{\text{MoE}}$$

其中 $\lambda_t$ 和 $\lambda_m$ 分别为三元组损失和 MoE 辅助损失的权重系数（实验中 $\lambda_t = 1.0$，$\lambda_m$ 依各辅助项独立配置，见第3章）。第4章中加入文本融合模块后，文本增强特征与原始视觉特征共享上述损失体系，无需额外引入独立的文本对比损失。

---

## 本章小结

&emsp;&emsp;本章系统介绍了本文方法所依赖的五类核心理论基础。

&emsp;&emsp;第2.1节对跨模态行人重识别任务进行了严格的形式化定义，明确了评估指标 mAP 与 Rank-$k$ 的计算方式，为后续实验结果的分析提供了统一的度量标准。第2.2节深入介绍了 CLIP 的对比预训练机制与 ViT 的架构细节，着重阐明了 Patch Token 序列作为多尺度特征提取输入来源的理论依据，以及 CLIP 联合语义空间为文本语义引导提供的先天优势。第2.3节介绍了 Mamba 的选择性扫描机制与 MM-SS2D 的多模态融合设计，说明了本文以 MambaPro 的 AAM 模块作为三模态融合基础的技术合理性。第2.4节系统阐述了 MoE 的条件计算原理、门控网络设计、专家结构及专家均衡辅助损失，为第3章基于 MoE 的多尺度动态融合方法提供了完整的理论支撑。第2.5节介绍了标签平滑交叉熵、在线难样本挖掘三元组损失和 BNNeck 归一化颈部，构建了本文完整的训练目标函数体系。

&emsp;&emsp;上述理论共同构成了本文方法的技术基石，后续第3章和第4章将在此基础上分别展开本文两项核心创新的详细设计与实验验证。

---

## 参考文献（第2章引用）

[1] Radford A, Kim J W, Hallacy C, et al. Learning transferable visual models from natural language supervision[C]//International Conference on Machine Learning. 2021: 8748-8763.

[2] Dosovitskiy A, Beyer L, Kolesnikov A, et al. An image is worth 16×16 words: Transformers for image recognition at scale[C]//International Conference on Learning Representations. 2021.

[3] Gu A, Goel K, Ré C. Efficiently modeling long sequences with structured state spaces[C]//International Conference on Learning Representations. 2022.

[4] Gu A, Dao T. Mamba: Linear-time sequence modeling with selective state spaces[J]. arXiv preprint arXiv:2312.00752, 2023.

[5] Yang Q, Wu A, Zheng W S. MambaPro: Multimodal multi-granularity non-linear pooling for RGB-infrared person re-identification with Mamba[J]. arXiv preprint, 2024.

[6] Jacobs R A, Jordan M I, Nowlan S J, et al. Adaptive mixtures of local experts[J]. Neural Computation, 1991, 3(1): 79-87.

[7] Szegedy C, Vanhoucke V, Ioffe S, et al. Rethinking the Inception architecture for computer vision[C]//Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition. 2016: 2818-2826.

[8] Hermans A, Beyer L, Leibe B. In defense of the triplet loss for person re-identification[J]. arXiv preprint arXiv:1703.07737, 2017.

[9] Luo H, Gu Y, Liao X, et al. Bag of tricks and a strong baseline for deep person re-identification[C]//Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition Workshops. 2019.

---

*本章字数统计：约 5 600 字（含参考文献）*
*下一章：第3章 基于多尺度滑动窗口与混合专家网络的跨模态行人重识别方法*
