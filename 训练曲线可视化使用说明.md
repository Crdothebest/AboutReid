# 训练曲线可视化使用说明

## 功能概述

`plot_training_curves.py` 脚本可以从训练日志文件中提取指标并绘制epoch折线图，帮助您可视化训练过程。

## 支持的指标

### 训练指标
- **训练损失 (Loss)**: 每个epoch的训练损失
- **训练准确率 (Accuracy)**: 每个epoch的训练准确率
- **学习率 (Learning Rate)**: 每个epoch的学习率变化

### 验证指标
- **mAP**: 平均精度均值
- **Rank-1**: Rank-1准确率
- **Rank-5**: Rank-5准确率
- **Rank-10**: Rank-10准确率

## 使用方法

### 基本用法

```bash
python plot_training_curves.py --log_file <日志文件路径>
```

### 示例

```bash
# 绘制基线实验的训练曲线
python plot_training_curves.py --log_file outputs/baseline_experiment/train_log.txt

# 绘制MoE实验的训练曲线
python plot_training_curves.py --log_file outputs/moe_innovation_experiment/train_log.txt

# 指定输出路径
python plot_training_curves.py --log_file outputs/baseline_experiment/train_log.txt --output results/training_curves.png

# 仅保存图片，不显示
python plot_training_curves.py --log_file outputs/baseline_experiment/train_log.txt --no_show
```

## 参数说明

| 参数 | 说明 | 必需 | 默认值 |
|------|------|------|--------|
| `--log_file` | 训练日志文件路径 (train_log.txt) | ✅ 是 | - |
| `--output` | 输出图片保存路径 | ❌ 否 | 日志文件同目录下的 `training_curves.png` |
| `--no_show` | 不显示图片，仅保存 | ❌ 否 | False (会显示图片) |

## 输出说明

脚本会生成一个包含6个子图的综合图表：

1. **训练损失曲线**: 显示训练损失随epoch的变化
2. **训练准确率曲线**: 显示训练准确率随epoch的变化
3. **学习率变化曲线**: 显示学习率调度情况（对数刻度）
4. **验证集 mAP 曲线**: 显示验证集mAP随epoch的变化
5. **验证集 Rank-k 准确率曲线**: 同时显示Rank-1、Rank-5、Rank-10
6. **训练损失与准确率对比**: 双y轴显示损失和准确率的对比

## 日志格式要求

脚本会自动解析以下格式的日志：

### 训练日志格式
```
Epoch[1] Iteration[10/100] Loss: 5.234, Acc: 0.123, Base Lr: 5.00e-04
```

### 验证日志格式
```
Validation Results - Epoch: 10
mAP: 45.2%
CMC curve, Rank-1  :45.2%
CMC curve, Rank-5  :65.8%
CMC curve, Rank-10 :75.3%
```

## 依赖安装

确保已安装以下Python包：

```bash
pip install matplotlib numpy
```

## 注意事项

1. **日志文件路径**: 确保日志文件路径正确，脚本会自动检查文件是否存在
2. **数据完整性**: 如果某个epoch没有验证结果，对应的验证指标可能为空
3. **中文显示**: 脚本已配置中文字体支持，如果显示乱码，请确保系统安装了中文字体
4. **图片保存**: 默认保存为PNG格式，分辨率为300 DPI，适合论文使用

## 示例输出

运行脚本后，您会看到：

```
📊 正在解析日志文件: outputs/baseline_experiment/train_log.txt

📈 提取到的指标统计:
  训练数据: 60 个epoch
  损失范围: 0.5234 ~ 5.2341
  准确率范围: 0.1234 ~ 0.9876
  验证数据: 6 个epoch
  mAP范围: 0.4523 ~ 0.6789
  Rank-1范围: 0.5234 ~ 0.7890

🎨 正在绘制训练曲线...
✅ 图片已保存至: outputs/baseline_experiment/training_curves.png

✅ 完成！
```

## 高级用法

### 批量处理多个实验

可以编写简单的shell脚本来批量处理：

```bash
#!/bin/bash
# batch_plot.sh

experiments=(
    "outputs/baseline_experiment"
    "outputs/moe_innovation_experiment"
)

for exp_dir in "${experiments[@]}"; do
    log_file="$exp_dir/train_log.txt"
    if [ -f "$log_file" ]; then
        echo "处理: $exp_dir"
        python plot_training_curves.py --log_file "$log_file" --no_show
    fi
done
```

### 对比多个实验

如果需要对比多个实验的曲线，可以修改脚本添加多实验对比功能，或者分别运行后手动对比生成的图片。

## 故障排除

### 问题1: 找不到日志文件
**错误**: `FileNotFoundError: 日志文件不存在: ...`

**解决**: 检查日志文件路径是否正确，使用绝对路径或相对路径

### 问题2: 没有提取到数据
**现象**: 显示"训练数据: 0 个epoch"

**解决**: 
- 检查日志文件格式是否符合要求
- 确认日志文件包含训练记录
- 查看日志文件内容，确认格式正确

### 问题3: 中文显示乱码
**现象**: 图表中的中文显示为方块

**解决**: 
- macOS: 确保安装了中文字体
- Linux: 安装中文字体包 `sudo apt-get install fonts-wqy-zenhei`
- 或者修改脚本中的字体设置

## 联系与反馈

如有问题或建议，请查看项目文档或提交Issue。

